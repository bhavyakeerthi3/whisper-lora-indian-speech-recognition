model_name_or_path: openai/whisper-tiny
processed_manifest: outputs/processed_manifest
output_dir: outputs/whisper-tiny-indic-lora-smoke
language: multilingual
task: transcribe
train_split: train
eval_split: validation
max_train_samples: 1
max_eval_samples: 1
per_device_train_batch_size: 1
per_device_eval_batch_size: 1
gradient_accumulation_steps: 1
learning_rate: 0.0001
num_train_epochs: 1
max_steps: 1
warmup_steps: 0
logging_steps: 1
eval_steps: 1
save_steps: 1
generation_max_length: 64
fp16: false
bf16: false
lora:
  r: 4
  alpha: 8
  dropout: 0.05
  target_modules:
    - q_proj
    - v_proj

